import Maintenance from "./maintenance";

// Simple wrapper for contractor maintenance that uses the same component as homeowners
export default function ContractorMaintenance() {
  return <Maintenance />;
}